pipeline {

    agent: any

    stages {
        stage("init") {
            script {
                echo 'initializing...'
            }
        }

        stage("test") {
            script {
                echo 'testing...'
            }
        }

        stage("build image") {
            script {
                echo 'building image...'
            }
        }
    }
}